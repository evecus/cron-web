name: Build CronPanel

on:
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    name: Build Binaries
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Get version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          else
            echo "VERSION=$(date +'%Y%m%d')-$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          fi

      - name: Build linux/amd64
        run: |
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build \
            -ldflags="-s -w -X main.Version=${{ steps.version.outputs.VERSION }}" \
            -o dist/cronpanel-linux-amd64 .

      - name: Build linux/arm64
        run: |
          GOOS=linux GOARCH=arm64 go build \
            -ldflags="-s -w -X main.Version=${{ steps.version.outputs.VERSION }}" \
            -o dist/cronpanel-linux-arm64 .

      - name: Show binary info
        run: |
          echo "📦 Build artifacts:"
          ls -lh dist/
          file dist/*

      # 每次 push 都上传 artifact（方便下载测试）
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cronpanel-${{ steps.version.outputs.VERSION }}
          path: dist/
          retention-days: 30

  # 仅在打 tag 时创建 Release
  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Get tag version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build all binaries
        run: |
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build \
            -ldflags="-s -w -X main.Version=${{ steps.version.outputs.VERSION }}" \
            -o dist/cronpanel-linux-amd64 .
          GOOS=linux GOARCH=arm64 go build \
            -ldflags="-s -w -X main.Version=${{ steps.version.outputs.VERSION }}" \
            -o dist/cronpanel-linux-arm64 .

      - name: Generate checksums
        run: |
          cd dist
          sha256sum cronpanel-linux-amd64 > cronpanel-linux-amd64.sha256
          sha256sum cronpanel-linux-arm64 > cronpanel-linux-arm64.sha256
          echo ""
          echo "SHA256 Checksums:"
          cat *.sha256

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.VERSION }}
          name: "CronPanel ${{ steps.version.outputs.VERSION }}"
          body: |
            ## CronPanel ${{ steps.version.outputs.VERSION }}

            现代化 Linux Crontab 定时任务 Web 管理面板

            ### 下载

            | 平台 | 架构 | 文件 |
            |------|------|------|
            | Linux | amd64 (x86_64) | `cronpanel-linux-amd64` |
            | Linux | arm64 (ARMv8) | `cronpanel-linux-arm64` |

            ### 快速使用

            ```bash
            # amd64
            chmod +x cronpanel-linux-amd64
            ./cronpanel-linux-amd64

            # arm64
            chmod +x cronpanel-linux-arm64
            ./cronpanel-linux-arm64
            ```

            默认端口 **8899**，访问 http://localhost:8899

            自定义端口：`PORT=9090 ./cronpanel-linux-amd64`

            ### 功能
            - ✅ 查看/添加/删除/启停 crontab 任务
            - ✅ 多种调度模式：每天、每N天、每周、每月、自定义 Cron 表达式
            - ✅ 支持直接命令、Shell 脚本路径、在线编写 Shell 脚本
            - ✅ 现代化深色主题 Web UI
          files: |
            dist/cronpanel-linux-amd64
            dist/cronpanel-linux-arm64
            dist/cronpanel-linux-amd64.sha256
            dist/cronpanel-linux-arm64.sha256
          draft: false
          prerelease: false
